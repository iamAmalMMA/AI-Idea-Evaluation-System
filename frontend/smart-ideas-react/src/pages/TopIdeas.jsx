import { Award, Star } from 'lucide-react'; import PageTitle from '../components/PageTitle';
export default function TopIdeas({ideas,onOpen}){const top=ideas.filter(i=>i.score).sort((a,b)=>b.score-a.score).slice(0,5);return <div className="page-container inner-page"><PageTitle title="أفضل 5 أفكار" description="ترتيب الأفكار حسب نتيجة التقييم الذكي."/><div className="ranking-list">{top.map((i,n)=><button key={i.id} className="ranking-card" onClick={()=>onOpen(i.id)}><div className="ranking-number">{n===0?<Award size={24}/>:n+1}</div><div className="ranking-copy"><h3>{i.title}</h3><p>{i.department} — {i.category}</p><div className="score-progress"><span style={{width:`${i.score*10}%`}}/></div></div><strong>{i.score}<Star size={18} fill="currentColor"/></strong></button>)}</div></div>}
