import { Bell, CheckCheck } from 'lucide-react'; import PageTitle from '../components/PageTitle';
export default function Notifications({items,onReadAll}){return <div className="page-container inner-page"><PageTitle title="الإشعارات" description="آخر التحديثات المتعلقة بأفكارك." action={<button className="ghost-button" onClick={onReadAll}><CheckCheck size={18}/>تحديد الكل كمقروء</button>}/><div className="notifications-list">{items.map(n=><article className={`notification-card ${n.read?'':'unread'}`} key={n.id}><div className="notification-icon"><Bell size={21}/></div><div><h3>{n.title}</h3><p>{n.message}</p><small>{n.time}</small></div></article>)}</div></div>}
